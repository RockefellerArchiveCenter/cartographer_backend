DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'postgres',
        'USER': 'postgres',
        'HOST': 'cartographer-db',
        'PORT': 5432,
    }
}

AWS_DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'postgres',
        'USER': 'postgres',
        'HOST': 'localhost',
        'PORT': 5432,
    }
}

# ArchivesSpace configs
ASPACE = {
  "baseurl": "http://sandbox.archivesspace.org:8089/",
  "username": "admin",
  "password": "admin",
  "repo_id": 2,
}

ALLOWED_HOSTS = ['cartographer-backend', 'localhost']

STATIC_URL = '/static/'

CORS_ORIGIN_WHITELIST = ('http://localhost:3000',)
